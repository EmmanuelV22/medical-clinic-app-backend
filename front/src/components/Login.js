import React from "react";

const Login = () => {
  return (
    <div className="">
      <form>
        <input type="email" />
        <input type="password" />
        <button>Iniciar sesión</button>
      </form>
    </div>
  );
};

export default Login;
