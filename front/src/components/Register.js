import React from "react";

const Register = () => {
  return (
    <div>
      <form>
        <input type="email" />
        <input type="password" />
        <button>Iniciar sesión</button>
      </form>
    </div>
  );
};

export default Register;
